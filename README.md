<div align="center">
  <h1>Seminar Codelab React</h1>
</div>

### Install Dependencies
- Install project dependencies: `npm install`

### Development (For Developer)
- Run: `npm start`
- Open browser at: `http://localhost:8080`

### Production Build
- Run: `npm run build`
- Copy all content in `./build` for deployment
